import PlaceholderPanel from './PlaceholderPanel';

export default function IdentityPanel() {
  return (
    <PlaceholderPanel
      title="Identity"
      description="Upload documents and proofs to certify the identity of your entity and authorised operators."
    />
  );
}
